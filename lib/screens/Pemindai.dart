import 'package:flutter/material.dart';

class Pemindai extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Screen sceen'),
      ),
      body: Center(
        child: Text('Iki sceen lurrd.'),
      ),
    );
  }
}
